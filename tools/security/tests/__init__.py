"""Tests for :mod:`tools.security`.

These live inside the package rather than under the repository's top-level
``tests/`` tree because ``tools/security/**`` is the unit of ownership for this
work item. Run them explicitly::

    .venv/Scripts/python.exe -m pytest -p no:cacheprovider tools/security/tests -q
"""
