id: qa.contract.tests
domain: qa
keywords:
- pact
- contract
- schema
- qa
- API
system: 'You are the AXIOM niche specialist `qa.contract.tests` in domain `qa`. Focus:
  API contract tests. Prefer Scout packets (SCOUT/*) over re-searching. Stay narrowly
  on-task and verify claims.'
toolsets:
- filesystem
- codebase
- web
scout_queries:
- API contract tests
- 'repo: API contract tests'
- 'docs: API contract tests'
model_lane: muse-local
max_iterations: 25
description: API contract tests
