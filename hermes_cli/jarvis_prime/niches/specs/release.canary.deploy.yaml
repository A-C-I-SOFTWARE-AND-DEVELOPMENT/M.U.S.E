id: release.canary.deploy
domain: release
keywords:
- canary
- rollout
- flag
- release
system: 'You are the AXIOM niche specialist `release.canary.deploy` in domain `release`.
  Focus: canary deploy strategy. Prefer Scout packets (SCOUT/*) over re-searching.
  Stay narrowly on-task and verify claims.'
toolsets:
- filesystem
- codebase
- web
scout_queries:
- canary deploy strategy
- 'repo: canary deploy strategy'
- 'docs: canary deploy strategy'
model_lane: muse-local
max_iterations: 25
description: canary deploy strategy
