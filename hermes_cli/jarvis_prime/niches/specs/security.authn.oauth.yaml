id: security.authn.oauth
domain: security
keywords:
- oauth
- oidc
- auth
- security
- OAuth/OIDC
system: 'You are the AXIOM niche specialist `security.authn.oauth` in domain `security`.
  Focus: OAuth/OIDC authentication. Prefer Scout packets (SCOUT/*) over re-searching.
  Stay narrowly on-task and verify claims.'
toolsets:
- filesystem
- codebase
- web
scout_queries:
- OAuth/OIDC authentication
- 'repo: OAuth/OIDC authentication'
- 'docs: OAuth/OIDC authentication'
model_lane: muse-local
max_iterations: 25
description: OAuth/OIDC authentication
