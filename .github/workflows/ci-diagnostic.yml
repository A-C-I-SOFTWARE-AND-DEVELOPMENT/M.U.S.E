name: CI Diagnostic

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: [self-hosted, primary]
    timeout-minutes: 5
    steps:
      - run: echo "Self-hosted runner is working!"
      - run: python --version
      - name: Check git
        run: git --version
