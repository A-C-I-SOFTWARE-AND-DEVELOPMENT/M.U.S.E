name: supabase
version: 0.1.0
description: "Supabase-backed persistent memory — stores conversation turns in a Postgres table and recalls recent context across sessions via PostgREST (recency + keyword; semantic/pgvector search is a follow-up). Uses httpx (a base dep); no SDK required."
