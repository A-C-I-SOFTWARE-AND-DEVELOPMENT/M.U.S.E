# Changelog

All notable changes to M.U.S.E are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

Auto-generated by [Release Drafter](https://github.com/release-drafter/release-drafter)
from merged PRs. Manual edits are accepted but will be overwritten on the next
release draft update.

## [v1.3.0] — 2026-07-27

### 🚀 Features
- **AAA Game Pipeline** — complete 11-stage pipeline with local toolchain support
  (decompose → previs → world_systems → creature_rig → blender_post →
  asset_generation → ue5_source → provenance → validation → acceptance)
- **Self-hosted runner** — `echer-windows-runner` registered and operational
- **CI automation suite** — pre-commit hooks, CODEOWNERS, auto-labeler,
  welcome bot, auto-format, stale bot, release drafter

### 🔒 Security
- Fixed 125/127 Dependabot vulnerabilities across pip + npm ecosystems
- Added `[tool.uv]` override-dependencies for 7 transitive security fixes
- Branch protection now requires ruff + secret scan + tests before merge
- `.gitattributes` forces LF on all lockfiles to prevent CRLF corruption

### 🔧 CI/CD
- All workflows switched from GitHub-hosted to `[self-hosted, primary]`
- Pinned `ruff==0.15.10`, `ty==0.0.48`, `uv==0.11.28` across all workflows
- `defaults.run.shell: bash` for cross-platform compatibility
- 98% test pass rate (30,518 tests) on Windows self-hosted runner
- Disabled Linux-only workflows (Nix, desktop) that can't run on Windows

### 🧹 Maintenance
- Pruned 317 stale remote branches (324 → 7)
- Git GC: 416MB → 222MB pack, 99MB garbage eliminated
- Reclaimed 41GB disk from AAA pipeline artifacts
- Deleted 2000+ old CI artifacts (storage quota exhausted)
- Cleaned stale worktrees and local branches

## [v1.2.0] — 2026-07-21

- Unified agent pool (670+ autonomous agents)
- AXIOM kernel + Swarm Grainler Parallel integration
- Dashboard Fusion + MoA tabs, full-page chat
