#!/usr/bin/env bash
# Legacy entry point: scripts/hermes-gateway was renamed to scripts/muse-gateway
# (Hermes -> MUSE rename). Permanent forwarding stub.
exec "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/muse-gateway" "$@"
