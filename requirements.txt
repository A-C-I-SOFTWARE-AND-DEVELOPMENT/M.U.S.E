# Vercel-only dependency manifest for the public cloud server (vercel_app.py).
#
# NOTE: this is a SECONDARY / fallback manifest. The PRIMARY mechanism that
# makes fastapi available on Vercel is the `[tool.vercel.scripts] build` hook in
# pyproject.toml (`uv sync --frozen --extra web`), because Vercel's FastAPI
# builder is uv-based and installs from pyproject/uv.lock — it does NOT read
# this requirements.txt. This file is kept only for the pip-based Python runtime
# path (non-FastAPI-framework), where a root requirements.txt IS consulted.
#
# It is NOT the source of truth for local/dev/CI installs — those use
# pyproject.toml + uv.lock (`uv sync`, `pip install -e '.[all,dev]'`). Do not
# add general runtime deps here; keep it to exactly what the public cloud
# server imports. Versions are pinned to match the `web` extra and the core
# `pydantic` pin in pyproject.toml so the cloud server tracks the rest of the
# project.
fastapi==0.133.1
starlette==1.3.1
pydantic==2.12.5
